export default function removeSecondsAndMilliseconds(timestamp) {
    // Split the timestamp by the ':' to separate hours, minutes, and seconds
    // const [hours, minutes] = timestamp.split(':');
    
    // Return only hours and minutes
    // return `${hours}:${minutes}`;
  }
  
 